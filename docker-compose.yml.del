#version: "3" #was 2.2

services:

    # Local Granite LLM
    granite:
        image: redhat/granite-7b-lab-gguf
        ports:
        - 8080:8080
        networks:
            - mynetwork
        command: -s    


networks:
    mynetwork:
        name: mynetwork
volumes:
    data:
    es-config:
            name: es-config